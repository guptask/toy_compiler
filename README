+------------+
|   README   |
+------------+

To build the code, type   :  make

To build the ctags, type  :  ./make-ctags

To execute the code, type : ./compile <code_file>


Notes:
-----

1. 'code' and 'code2' are sample files in this directory.

2. For special cases not mentioned in guidelines, 
   C-type rules have been followed eg. re-using of 
   local variable names in different procedures, etc.

